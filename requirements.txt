# headless: el build de Railway no trae las libs de GUI (libxcb, libGL) que pide
# `opencv-python`. Ni la API ni `dados.py`/`main.py` usan cv2.imshow (el paso a
# paso se dibuja con matplotlib en backend Agg), asi que no se pierde nada.
opencv-python-headless
matplotlib

# Capa HTTP (api/): expone la solucion como microservicio.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
python-dotenv>=1.0

{
  "$schema": "https://schema.railpack.com",
  "deploy": {
    "startCommand": "PY=${VIRTUAL_ENV:-/app/.venv}/bin/python; [ -x \"$PY\" ] || PY=python; exec \"$PY\" -m uvicorn api.main:app --host 0.0.0.0 --port ${PORT:-8002}"
  }
}
